/**
 * Clase que representa una dirección en el espacio bidimensional.
 * @param {number} dX - La coordenada x de la dirección.
 * @param {number} dY - La coordenada y de la dirección.
 */
export class Direction
{
    constructor(dX, dY) {
        this.dX = dX;
        this.dY = dY;
    }
}